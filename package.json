{
    "private": true,
    "workspaces": {
        "packages": [
            "apps/reprocess-mediator",
            "apps/reprocessor-mediator-microfrontend"
        ],
        "nohoist": [
            "**/react",
            "**/react/**",
            "**/react-dom",
            "**/react-dom/**"
        ]
    },
    "license": "MIT",
    "scripts": {
        "start": "yarn workspaces run start",
        "build": "yarn workspaces run build"
    },
    "devDependencies": {
        "yarn": "^1.0.0"
    }
}
